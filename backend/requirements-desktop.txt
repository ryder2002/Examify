-r requirements-base.txt
pytesseract==0.3.13
pdf2image==1.17.0
opencv-python-headless==4.10.0.84
# Keep a wheel-based release so the Windows installer does not need a Rust
# toolchain or a compiler during sidecar packaging.
cryptography==44.0.3
pyinstaller==6.14.2
